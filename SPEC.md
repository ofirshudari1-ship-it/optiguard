# מסמך אפיון — OptiGuard

**גרסה נוכחית:** 4.10
**תגית:** All-In-One PC Care
**זכויות:** © Ofir Shudari - כל הזכויות שמורות

---

## 1. תקציר מנהלים

OptiGuard היא תוכנת תחזוקת מחשב "הכל באחד" ל-Windows, שנבנתה סביב עיקרון אחד שחסר ברוב השוק: **לעולם לא לזייף תוצאות, לעולם לא לגזול, ותמיד לתת דרך חזרה**. במקום מנוע רישום גנרי שסורק הכל וטוען שמצא "14,782 שגיאות קריטיות" (טקטיקת הפחדה נפוצה בשוק), OptiGuard מבצע רק בדיקות ממוקדות ומבוססות-ראיות, מציג בדיוק למה כל פריט סומן, ושומר רשת ביטחון (הסגר של 7 ימים + נקודות שחזור אוטומטיות) לפני כל פעולה מסוכנת.

התוכנה משלבת הסרת תוכנות מלאה, ניקוי מערכת, מרכז אבטחה ופרטיות, וכלי אבחון - הכל תחת ממשק אחד, בעברית ואנגלית, עם ציון בריאות מחשב אחד שמסכם את מצב המערכת במבט אחד.

---

## 2. הבעיה שהמערכת פותרת

### 2.1 הסרת תוכנות לא מלאה
כשמסירים תוכנה דרך "הוספה/הסרה" הרגיל של Windows, נשארים מאחור: תיקיות ב-`%APPDATA%`, ערכי רישום, קיצורי דרך, קבצי הגדרות. עם הזמן זה מצטבר לזבל דיגיטלי שמאט את המערכת ותופס מקום.

### 2.2 כלי ניקוי שמפחידים כדי למכור
מחקר שוק (ראו סעיף 4) מראה שרוב כלי ה-PC Cleaner בשוק בנויים סביב הפחדת המשתמש ("נמצאו 14,782 שגיאות!") כדי לדחוף שדרוג בתשלום, כולל ניקוי רישום עיוור שלעיתים **שובר תוכנות עובדות**.

### 2.3 כלי "עדכון דרייברים" הם קטגוריית תוכנה זדונית מוכרת
כלים כמו "Driver Booster" מסומנים כ-PUP (Potentially Unwanted Program) על ידי כלי אנטי-וירוס, ו"WinZip Driver Updater" הוא למעשה malware שגונב פרטים. OptiGuard נמנע לגמרי מהקטגוריה הזו.

### 2.4 חוסר שקיפות במה שהתוכנה עושה בפועל
משתמשים לא יודעים אם ניקוי "עבד", לא רואים היסטוריה של פעולות, ולא מבינים למה תוכנה מסוימת נחשבת "מסוכנת" להסרה.

### 2.5 חוסר נגישות
רוב כלי התחזוקה לא חושבים על משתמשים עם צרכי נגישות (ניגודיות גבוהה, מקלדת בלבד).

---

## 3. למה כדאי להשתמש ב-OptiGuard

| עיקרון | איך זה בא לידי ביטוי |
|---|---|
| **בטיחות לפני הכל** | כל ניקוי עובר דרך הסגר של 7 ימים (ניתן לביטול), ונקודת שחזור נוצרת אוטומטית לפני פעולות מסוכנות |
| **דיוק, לא הפחדה** | ניקוי רישום מסמן רק רשומות עם הוכחה קונקרטית שהנתיב שאליו הן מצביעות כבר לא קיים - לא סריקה עיוורת של כל הרישום |
| **שקיפות מלאה** | ציר זמן פעילות אמיתי, לוג מפורט, ציון בריאות עם הסבר לכל בעיה וכפתור "תקן זאת" ישיר |
| **בלי הפחדות, בלי הצעות חבילה** | אין פרסומות, אין "שדרג ל-Pro", אין איסוף נתונים, אין ניפוח מספרי "שגיאות" |
| **מבוסס על מנועי Windows אמיתיים** | אבטחה = Windows Defender האמיתי, לא מנוע מזויף; דרייברים = Device Manager האמיתי, לא הורדה ממאגר לא מאומת |
| **נגיש לכולם** | ערכת ניגודיות גבוהה ייעודית לנגישות, פלטת פקודות למקלדת (Ctrl+K), הכל דו-לשוני (עברית/אנגלית) עם RTL מלא |
| **חכם, לא רק כלי** | ציון בריאות מחשב שמצרף מספר מקורות נתונים שונים למספר אחד מובן, עם קישור ישיר לתיקון כל בעיה |

---

## 4. מחקר שוק שהניע החלטות עיצוב

לאורך הפיתוח בוצע מחקר מתחרים (CCleaner, IObit Advanced SystemCare, Glary Utilities, Wise Care 365) שהוביל להחלטות מפורשות:

- **נמצא חסר ונבנה:** מנתח שטח דיסק, מוצא כפילויות קבצים - פיצ'רים נפוצים בתעשייה שחסרו
- **נמצא מסוכן ונמנע במכוון:** "עדכון דרייברים" אוטומטי (קטגוריית scareware מוכרת), "ניקוי RAM" ו"דפרגמנטציית רישום" (פסאודו-מדע ב-Windows מודרני)
- **אומת שהמיצוב הנכון:** כלים שמזייפים תוצאות/מפחידים משתמשים מקבלים ביקורת שלילית עקבית - האסטרטגיה של OptiGuard (שקיפות, דיוק, בלי הפחדות) מאומתת כנכונה

### 4.1 סבב מחקר נוסף (4.10, 2026-09-16)

מחקר ממוקד מול CCleaner, IObit Uninstaller, Revo Uninstaller, BleachBit ו-Windows Storage Sense, בהמשך למחקר הקודם:

- **נמצא חסר ונבנה:** ניקוי אוטומטי מתוזמן (CCleaner ו-IObit Uninstaller שניהם מציעים) - OptiGuard היה ידני בלבד עד כה; ציון ביטחון "בטוח להסרה" מבוסס תאריך שימוש אחרון (כמה כלי הסרה מציגים) - OptiGuard לא הציג אות דומה
- **נמצא כבר קיים ולא שוכפל:** מנהל הפעלה אוטומטית, ניקוי תוספי דפדפן, מנתח שטח דיסק/קבצים גדולים, מוצא כפילויות - כל אלה כבר קיימים ב-OptiGuard מהמחקר הקודם, לא נבנו מחדש
- **נמצא מסוכן ונמנע שוב במכוון:** אותה מסקנה כמו קודם (מעדכני דרייברים, ניקוי RAM/דפרגמנטציית רישום) - לא השתנה

---

## 5. איך המערכת עובדת — סקירה ארכיטקטונית

### 5.1 טכנולוגיה
- **שפה:** C# / .NET Framework 4.8, נבנה עם `dotnet build` דרך `src/OptiGuard.csproj` ו-`Setup.csproj` (SDK-style projects, לא csc.exe ידני)
- **ממשק:** WPF, נבנה כולו בקוד (ללא קבצי XAML נפרדים) דרך `XamlReader` לטעינת Styles/Templates כמחרוזות בזמן ריצה
- **הרשאות:** התוכנה דורשת הרצה כמנהל (מוגדר ב-manifest) כדי לגשת לרישום ברמת המערכת ול-WMI
- **התקנה:** קובץ EXE יחיד (WinForms) שמטמיע את קובץ האפליקציה הראשי כמשאב פנימי, מזהה ומחליף התקנות ישנות אוטומטית, ובודק אם התוכנה כבר פתוחה לפני התקנה/עדכון
- **גרסה:** מקור אמת יחיד ב-`version.json`, מוזרק לשני הפרויקטים דרך `build/Version.props` בזמן בנייה (ראו `build.ps1`), ונקרא בחזרה בזמן ריצה מ-Assembly version — אין מספר גרסה קשיח יותר בקוד

### 5.2 עקרון "הסגר" (Quarantine)
קבצים שמסומנים למחיקה **לא נמחקים מיד** - הם עוברים לתיקיית הסגר פנימית עם רישום (manifest) שמתעד את המיקום המקורי. "בטל ניקוי אחרון" משחזר אותם. אחרי 7 ימים, פריטים ישנים נמחקים לצמיתות אוטומטית. אותה אצווה (batch) נטענת ונשמרת **פעם אחת** (לא לכל קובץ בנפרד) כדי לשמור על ביצועים גם עם אלפי קבצים.

### 5.3 עקרון "נקודת שחזור"
לפני פעולות בעלות סיכון (ניקוי זבל, הסרת שאריות, ניקוי רישום), המערכת יוצרת נקודת שחזור Windows דרך WMI. כדי לא להאט פעולות חוזרות, נקודת שחזור חדשה מדולגת אם כבר נוצרה אחת ב-4 השעות האחרונות.

### 5.4 עקרון "חשיפת האמת, לא בנייה מחדש"
בכל מקום שבו קיים מנוע Windows מובנה אמין (Defender, חומת אש, Windows Update, Device Manager), OptiGuard **קורא את הסטטוס האמיתי שלו** ומציג אותו, במקום לבנות מנוע מקביל שעלול להטעות.

---

## 6. מפת פיצ'רים (18 עמודים פעילים)

### קבוצה: סקירה
| עמוד | תפקיד |
|---|---|
| **דשבורד** | ציון בריאות מחשב (0-100, מבוסס בדיקות אמיתיות), סטטיסטיקות מצטברות, 4 אשפים מונחים — מדחוס בפריסה כדי שהכל ייכנס במסך אחד בלי גלילה |
| **ציר זמן פעילות** | היסטוריה כרונולוגית אמיתית של כל ניקוי, הסרת תוכנה וגיבוי רישום - בנויה מנתונים קיימים, לא לוג טקסט גולמי |

### קבוצה: הסרת תוכנה
| עמוד | תפקיד |
|---|---|
| **תוכנות** | רשימת תוכנות מותקנות, הסרה רגילה/עמוקה/כפויה, ייצוא לקובץ CSV, זיהוי שאריות מבוסס "טביעת אצבע" (fingerprint) שנלכד בזמן ההתקנה, **ציון ביטחון "בטוח להסרה" (4.10)** מבוסס תאריך שימוש אחרון (Prefetch) |
| **משחקים** | זיהוי משחקים מותקנים (Steam/Epic/GOG ועוד), הסרה רשמית או כפויה |
| **תוספי דפדפן** | ניהול תוספים ב-Chrome/Edge/Firefox, **דירוג סיכון הרשאות** (גבוה/בינוני/נמוך) מבוסס ניתוח manifest |

### קבוצה: תחזוקה
| עמוד | תפקיד |
|---|---|
| **הפעלה אוטומטית** | ניהול תוכנות שמופעלות עם Windows |
| **ניקוי זבל** | קבצים זמניים, מטמון דפדפן, סל מיחזור ועוד - הכל דרך הסגר בר-שחזור; **ניקוי אוטומטי מתוזמן אופציונלי (4.10)** דרך מתזמן המשימות של Windows |
| **מנתח שטח דיסק** | פירוק ויזואלי של מה שתופס מקום, ניווט פנימה לתיקיות, איתור הקבצים הגדולים בכונן |
| **מוצא כפילויות** | השוואת קבצים לפי תוכן (SHA1, לא רק שם/גודל), שומר את העותק הישן כמקור |
| **תיקונים מהירים** | פעולות תחזוקה נפוצות בלחיצה אחת |

### קבוצה: עדכונים
| עמוד | תפקיד |
|---|---|
| **עדכוני תוכנה** | בדיקת עדכונים דרך winget (הקטלוג הרשמי של Microsoft) |

### קבוצה: מערכת
| עמוד | תפקיד |
|---|---|
| **מידע מערכת** | פרטי CPU/GPU/RAM/דיסקים/לוח אם + **בריאות מכשירים/דרייברים** (Device Manager אמיתי, לא הורדה) |
| **בריאות דיסק** | מצב S.M.A.R.T ומקום פנוי, סריקת chkdsk |

### קבוצה: אבטחה ופרטיות
| עמוד | תפקיד |
|---|---|
| **מרכז אבטחה** | סטטוס Windows Defender/חומת אש/Windows Update אמיתי + הפעלת סריקה מהירה |
| **פרטיות** | 7 מתגי פרטיות אמיתיים (אותם מפתחות רישום שהגדרות Windows עצמן משתמשות בהן) |
| **ניקוי רישום** | איתור רשומות "רפאים" מבוסס-ראיות בלבד, גיבוי .reg אוטומטי לפני הסרה |
| **בודק פישינג** | ניתוח היוריסטי של טקסט/קישורים חשודים (IP URLs, פונאיקוד, דומיינים דמויי-מותג) |

### נוספים
- **הגדרות:** שפה (עברית/English), ערכת נושא (בהיר/כהה/**ניגודיות גבוהה לנגישות**), נקודות שחזור, הסגר, התראות, יומן
- **אשף כניסה ראשונה:** בחירת שפה + ערכת נושא בהפעלה הראשונה בלבד

**הוסרו (4.8):** "אפליקציות Windows" (UWP) ו-"מעקב התקנה" (Install Monitor) הוסרו כעמודים עצמאיים - שימוש נמוך, מוסיפים בלבול. זיהוי טביעת-אצבע להתקנה נשאר פועל **מתחת למכסה המנוע** בעמוד "תוכנות" (זיהוי שאריות בהסרה), רק בלי ממשק משתמש נפרד.

---

## 7. עקרונות בטיחות ואבטחת מידע

1. **ללא טלמטריה** - כל הנתונים נשארים במחשב המקומי (`%APPDATA%\UninstallerPro`)
2. **ללא רשת חיצונית** - למעט בדיקת עדכונים (winget) ואבחון פישינג (לא שולח כלום החוצה)
3. **הרשאות מנהל נדרשות רק לפעולות שבאמת דורשות** - קריאת רישום ברמת מערכת, WMI
4. **כל פעולה הרסנית מתועדת בלוג** - ניתן לבדוק בדיוק מה קרה ומתי
5. **מתקין בודק מופע רץ לפני החלפת קבצים** - מונע נעילת קובץ באמצע עדכון; האפליקציה עצמה חסומה גם היא ל-instance יחיד (`Mutex`)

פירוט מלא בביקורת: `build/reports/SECURITY_AUDIT.md`.

---

## 8. מצב הפרויקט (גרסה 4.10)

**הושלם בסבב הנוכחי (4.10, 2026-09-16):** מחקר מתחרים ממוקד (סעיף 4.1) → שני
פיצ'רים חדשים שיישמו את הממצאים: ציון ביטחון "בטוח להסרה" מבוסס Prefetch
בעמוד תוכנות, וניקוי אוטומטי מתוזמן דרך Windows Task Scheduler (כבוי כברירת
מחדל, אופט-אין מלא). נסקרו מסלולי הסריקה/ניקוי הקריטיים לביצועים - לא נמצאו
בעיות חדשות מעבר למתועד בביקורות קודמות. `locales/*.json` עודכנו ל-527
מפתחות בשני הקבצים (מ-512), עדיין בזוגיות מלאה. ראו `CHANGELOG.md` לפירוט
מלא כולל קבצים.

התוכנה עברה מחזור פיתוח אינטנסיבי הכולל: בניית הליבה, הרחבת פיצ'רים מבוססת-מחקר, מעבר UI/UX לניווט צידי מקובץ, תיקון באג ביצועים/אובדן-נתונים קריטי במנגנון ההסגר, סבב התאמות נגישות/היררכיית כפתורים, מעבר לבנייה מבוססת `dotnet build`/csproj (במקום csc.exe ידני), הוספת בדיקת מופע-רץ במתקין ובאפליקציה, ודחיסת ה-Dashboard כדי שייכנס במסך אחד. ראו `CHANGELOG.md` להיסטוריה מלאה.

**סטטוס:** יציב, מאומת מול נתונים אמיתיים בכל מודול.

**הושלם בסבב הנוכחי:** Splash screen (`SplashWindow.cs`, מאומת ויזואלית) + אשף הפעלה-ראשונה מלא בן 5 מסכים (`OnboardingWindow.cs`, כפתור דילוג גלוי + Esc, ניתן להריץ שוב מההגדרות) + זיכרון גודל/מיקום/מצב-מוגדל של החלון בין הפעלות + באנר עדכון לא-חוסם (תיקן הגדרה מתה: "בדיקת עדכונים אוטומטית" היה קיים בהגדרות בלי שום דבר שקורא אותו) + תרגום טקסט המתקין עצמו לעברית + כפתור "עיון" לשינוי תיקיית התקנה + שאלת מחיקת-נתונים בהסרה + דף נחיתה דו-לשוני מלא עם RTL ו-FAQ.

**הושלם בסבב נוסף (2026-09-15):** רכיב Toast חוזר לפי מפרט התקן (פינה עליונה, 340px, נסגר לבד אחרי 5 שניות, סגירה ידנית תמיד זמינה) - מחובר לניקוי הסגר ברקע שהיה שקט לגמרי קודם. **אותרו וטופלו 5 הגדרות מתות נוספות** (`MinimizeOnClose`, `ShutdownDelaySeconds`, `ConfirmDestructiveActions`, `CreateRestorePointsBeforeCleanup`, `EnableQuarantine`) שנשמרו/נטענו אך מעולם לא נקראו על ידי שום קוד - הוסרו במקום להישאר כ"תפאורה" (ראו `DELETIONS.md` לנימוק המלא של כל אחת). `QuarantineRetentionDays` תוקן במקביל - היה קיים אך מוחלף בקוד קשיח `7`; עכשיו בורר אמיתי בהגדרות. `GLOSSARY.md` נוצר.

**ידוע וממתין (ראה STANDARDS.md ב-`_AUDIT/`):** מיפוי RTL מלא ברמת פיקסל בעמודי המתקין עצמו (WinForms עם מיקום מוחלט לכל בקרה) - טקסט מתורגם ומיושר לימין בעברית, אך סדר הבקרות עצמו לא הוזז; תועד כפער ידוע ולא כפתרון-חלקי-מוסתר.
